#include "main.h"
/**
* _islower - Sheck for lowe c
* @c: contains value to be compared
* Return: 0 or 1 if condition is met
*/
int _islower(int c)
{

if (c > 'a' && c < 'z')
{
return (1);
}

else
{
return (0);
}

}
